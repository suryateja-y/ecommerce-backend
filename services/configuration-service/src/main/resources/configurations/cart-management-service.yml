server:
    port: 8087
application:
    kafka:
        consumer:
            order-consumer: order-consumer-cart-group-1
spring:
    data:
        mongodb:
            host: localhost
            port: 27017
            database: ${spring.application.name}
            authentication-database: admin
        redis:
            host: localhost
            port: 6379
    cache:
        redis:
            time-to-live: 300
